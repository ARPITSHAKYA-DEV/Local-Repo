This is My First repository.<hr>
if we check a current branch then use    git branch<hr>
if we want to create a branch then use   git checkout -b (branchname)<hr>
if we want switch a branch from one to another then use   git checkout (branch name)<hr>
if we want to delete a branch then first we will not pesent on same branch first move to another branch then use    git checkout -d (branch name)<hr>
if we change a branch name     git branch -M (changed name)<hr><hr><hr>
if we want to merge a code  way 1<hr>
check git differences both of them by using   git diff<branch name>(to campare commits ,branches,files &more)<hr>
git merge<branch name><hr><hr><hr>
way 2<hr>
create a PR(pull request)<hr>
after we merged a two branch if we want this change in your local then use a git pull origin main

