This is My First repository.
if we check a current branch then use    git branch
if we want to create a branch then use   git checkout -b (branchname)
if we want switch a branch from one to another then use   git checkout (branch name)
if we want to delete a branch then first we will not pesent on same branch first move to another branch then use    git checkout -d (branch name)
if we change a branch name     git branch -M (changed name) 